# 🍬 Loja Delícias da Thay (Exemplo)
Sistema web simples em PHP para demonstração de CRUD e integração com MySQL.

## Como rodar (modo rápido)
1. Coloque esta pasta dentro da raiz do seu servidor local (ex: htdocs do XAMPP).
2. Importe `db/loja.sql` no seu MySQL (opcional).
3. Ajuste `includes/conexao.php` com credenciais do banco.
4. Acesse `http://localhost/loja-doce-delicias/index.php`.
